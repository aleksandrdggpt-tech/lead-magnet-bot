# Lead Magnet Bot

Бот для раздачи лид-магнитов через кнопки в Telegram канале.

## Функционал

- ✅ Создание постов с кнопками в канале (только для админов)
- ✅ Проверка подписки на канал перед выдачей ссылки
- ✅ Раздача ссылок пользователям, перешедшим по кнопке
- ✅ Статистика нажатий на кнопки

## Установка

1. Клонируйте репозиторий или скопируйте файлы
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env`:
```env
BOT_TOKEN=your_bot_token_here
ADMIN_USER_IDS=123456789,987654321
CHANNEL_USERNAME=@TaktikaKutuzova
DATABASE_URL=postgresql://user:password@host:port/database
```

4. Запустите бота:
```bash
python bot.py
```

## Использование

### Для администраторов

1. `/admin` - открыть админ-панель
2. `/add_button` - создать пост с кнопкой в канале

### Процесс создания поста с кнопкой

1. Выполните команду `/add_button`
2. Введите текст кнопки (например: "Получить лид-магнит")
3. Выберите тип лид-магнита:
   - 🤖 Доступ к боту - кнопка ведет на бота
   - 🔗 Внешняя ссылка - кнопка ведет на внешний ресурс (Google Drive, опрос и т.д.)
4. Если выбрана внешняя ссылка - введите ссылку
5. Введите username канала (например: `@channel_username`)
6. Отправьте контент поста (текст, текст с фото, или переслать сообщение)

Бот опубликует пост в канале с кнопкой.

### Для пользователей

1. Пользователь нажимает кнопку в канале
2. Бот проверяет подписку на канал
3. Если подписан - выдает ссылку
4. Если не подписан - просит подписаться

## Структура проекта

```
.
├── bot.py                      # Основной файл бота
├── config.py                   # Конфигурация
├── requirements.txt            # Зависимости
├── database/                   # Модели базы данных
│   ├── __init__.py
│   ├── base_models.py         # Базовая модель User
│   ├── models.py              # Модели ChannelButton, ChannelButtonClick
│   └── database.py            # Подключение к БД
├── services/                   # Сервисы
│   └── channel_button_service.py
└── modules/                    # Модули
    └── payments/
        ├── admin_handlers.py   # Админ-панель
        ├── handlers.py         # Обработчики подписки
        ├── keyboards.py        # Клавиатуры
        ├── messages.py         # Сообщения
        └── subscription.py     # Проверка подписки
```

## База данных

Бот использует PostgreSQL (production) или SQLite (development).

Таблицы:
- `users` - пользователи
- `channel_buttons` - информация о кнопках в каналах
- `channel_button_clicks` - статистика нажатий на кнопки

## Развертывание

### Railway

1. Создайте новый проект на Railway
2. Подключите PostgreSQL базу данных
3. Установите переменные окружения:
   - `BOT_TOKEN`
   - `ADMIN_USER_IDS`
   - `CHANNEL_USERNAME`
   - `DATABASE_URL` (автоматически устанавливается Railway)
4. Установите Root Directory: `.` (корень проекта)
5. Deploy

### Локально

```bash
# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env

# Запустите бота
python bot.py
```

## Важные замечания

1. Бот должен быть администратором канала для работы с кнопками
2. Для проверки подписки бот должен иметь права администратора в канале
3. Канал указан в переменной окружения `CHANNEL_USERNAME`

## Лицензия

MIT
